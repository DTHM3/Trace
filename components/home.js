import * as React from 'react';
import { View, Text, Button, Alert } from 'react-native';

const Home = (props) => {

  return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Button title='Recovery!' onPress={ () => { }}/>
      </View>
  );
}

export default Home;